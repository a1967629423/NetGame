declare function factorial(num);
declare function morebsr(t:number, arg:number[]);
declare var DMath:{
    morebsr(t:number, arg:number[]),
    radianToAngle(r:number):number,
    pathCalcaulate(bx:number,by:number,ex:number,ey:number):{x:number,y:number,allLength:number,
        firstRadian:number,lastRadian:number,rectDir:boolean,rectH:number,rectW:number,Hypotenuse:number},

}